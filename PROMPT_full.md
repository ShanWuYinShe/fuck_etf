# A股ETF交易辅助 · 操作提示词

> 持仓 `config/holdings.json`、自选 `config/etf_list.txt` 是唯一数据源。采集命令、文件结构见 `AGENTS.md`。

---

## 一、每轮流程

1. 采集：`python3 scripts/fetch_etf_data.py`（盘后只刷快讯：`--news-only`）
2. 读：`config/holdings.json` → `config/etf_list.txt` → `.workwork/data/news_merged.json`（最新 30 条）→ `.workwork/data/news_ann.json` → `.workwork/data/boards_up.json` + `boards_down.json`（板块榜，见 2.1）→ `.workwork/data/_manifest.json`（fail 项标"⚠无数据"）
3. 算大盘环境（第二节），写在输出最上方
4. 每只持仓+自选输出分析行（第三节模板）
5. 末尾：持仓跟踪表 + 今日下单清单 + 150 字总结

---

## 二、大盘环境

读 `index_realtime.txt` + `index_kline_*.json`，算 20 日均线。

| 环境 | 判定 | 仓位 |
|---|---|---|
| **正常** | 指数 ≥ 20 日线 且 20 日线走平/向上 | 单标的 ≤1/3，核心机会 ≤1/2 |
| **谨慎** | 指数 < 20 日线 **或** 20 日线拐头向下 | 单标的 ≤1/4 |
| **保守** | 连续 3 日 < 20 日线 **或** 放量跌破前期平台 | 停新开仓，持仓减至半仓以下 |

成长/行业 ETF 持仓为主时，**上证+创指同时满足**才认定为"正常"。环境降级时旧低吸单按新仓位重算。

### 2.1 板块走势（每轮必读）

`boards_up.json` / `boards_down.json` 为东财**行业板块榜前 10 强 / 10 弱**，原始结构 `data.diff[]`：`f12` 板块代码、`f14` 板块名称、`f3` 涨跌幅（%）、`f62` 主力净流入（元）。**非交易时段 f3=0、f62="-" 属正常**，以最近交易日的涨跌结构为准。

- 每只标的先映射其所属行业板块（半导体/黄金/创新药/通信/军工/证券等），再看该板块是否在榜内、涨跌幅与主力净流入方向。
- 板块强度三级（叠加到本节大盘环境判定）：
  - **强**：所属板块在涨幅榜前列，或板块红盘 + 主力净流入
  - **中**：板块平盘（涨跌幅 ±1% 内），或不在榜内但板块指数与 ETF 联动正常
  - **弱/退潮**：所属板块在跌幅榜，或板块绿盘 + 主力净流出
- 大盘环境降级时板块强度一并降级；环境"正常"但持仓板块普遍退潮时，按"谨慎"处理该板块持仓。

---

## 三、每只标的输出模板

**先持仓、后自选**：

```
[T0/T1] 代码 名称 | 现价 X.XXX (涨跌幅%) | 区间 X.XXX-X.XXX | 均价 X.XXX | 量比 X.XX
板块:强/中/弱 (所属板块 + 涨跌幅% + 主力净流入)
模式:趋势/震荡 | 4 项日内(仅持仓): 委比+/-XX% 现手 X 换手 X.XX% 量比 X.XX
方向:看多/震荡偏多/震荡/偏空/看空 (N日)
低吸区:X.XXX-X.XXX | 目标区:X.XXX-X.XXX | 时间窗:N日 | 证伪:一句话
操作:动词 | 挂单价 X.XXX | 数量 X份 | 止损 X.XXX | 目标 X.XXX | 盈亏比 X.XX | 单型:股价条件/止盈止损/... | 触发:一句话
```

**前瞻 5 字段**（必出）：方向 / 低吸区 / 目标区 / 时间窗 / 证伪信号。

**前瞻三要素**：消息面 + 趋势结构 + **板块强度**（2.1）。板块退潮的品种方向一律降级（如偏多→震荡、震荡→偏空），低吸区下移/目标区下调；板块走强的品种可正常给方向。板块强度变化（转强/转弱）本身可作为证伪信号。

**盈亏比** = (目标 − 挂单价) ÷ (挂单价 − 止损)。

---

## 四、做 T

T+1 品种：先卖当日可卖份额，尾盘/次日等量买回（底仓不变）。
T+0 品种：可先买后卖或先卖后买。

4 项日内指标（委比、现手、换手、量比）从 `etf_realtime_qq.txt` 读，AI 据此判定动作（无硬阈值）。

---

## 五、硬规则

### 5.1 价位

**最小触发价差**（现价分档，元）：

| 现价 | <1元 | 1–2元 | 2–5元 | ≥5元 |
|---|---|---|---|---|
| 最小价差 | 0.001 | 0.002 | 0.004 | 0.008 |

- **单笔 ≥300 元**
- **价差一律用元（3 位小数），不用百分比**（唯一例外：日涨跌幅单型）
- **盈亏比**：新开仓 ≥1.2，看多+结构确认可放宽 ≥1.0；不达标只给持有/观望

### 5.2 止损

- **止损价** = 关键支撑（10 日低/前期平台/20 日线）下方 1 个最小触发价差档
- **回调**：缩量 + 守住关键支撑 + 板块仍强（2.1）+ 消息面未坏 → 持有
- **破位**：放量 **或** 跌穿关键支撑 **或** 板块逻辑走坏 **或** 板块退潮（连续多日在跌幅榜/主力持续净流出）**或** 多日连续下行无反抽（任一）→ 立即止损，不等 14:45
- **假破位**（次日收回支撑上方且放量修复）→ 接回价 ≤ 离场价+1 档

### 5.3 条件单

- **买入类**（股价条件买入/反弹买入）不写止损，只有触发价+方向+数量
- 止损由成交后另挂的"止盈止损卖出单"承担
- **同一批份额的止损只能由一张卖出类单承担**（止盈止损 vs 股价条件止损二选一）
- **买入以可成交为前提**（低吸贴价 / 突破轻仓追 ≤1/4 仓），**禁市价追日内高点**

### 5.4 T+1 卖出份额

- 当日可卖份额 = `buy_time` 非今日批次之和
- `buy_time`=今日的批次当日不可卖，触发减仓/止损/止盈价时**延至下一交易日执行**

### 5.5 做 T

- **成对**：买入价 + 卖出价 + 预期价差，三项同时给
- 盈亏比 ≥1 才推荐
- 底仓不变；T+1 卖出量 ≤ 当日可卖份额
- 每日 ≤2-3 次/标的
- **禁追高买回**——卖出后价格不回落，宁可不补回

### 5.6 输出标注

- **每行条件单标代码+名称+单型+全参数**
- **涨跌幅 >2% 标⚠异动**；fail 项标⚠无数据
- **禁裸低吸**（买入必给目标或做 T 卖价）

---

## 六、消息面

- 关键事件 1-3 条（标来源+时间）
- 利多/利空逐条标，输出"⚡利多：... ⚠利空：..."
- 权威源 > 二手快讯；`news_ann.json` 个股公告优先
- 与价格背离时**以消息面为主**

---

## 七、末尾输出

### 7.1 持仓跟踪表

| 代码 | 名称 | 总份额 | 加权成本 | 今日可卖/锁定 | 浮盈% | 处理线 |
|---|---|---|---|---|---|---|

### 7.2 今日下单清单

| 账号 | 代码 | 名称 | 方向 | 挂单价 | 数量 | 单型 | 止损 | 目标 | 盈亏比 | 触发条件 |
|---|---|---|---|---|---|---|---|---|---|---|

- 观望行：挂单价"-"，触发条件写明重新介入价
- 低吸预挂单：备注"低吸预挂单"
- 做 T：成对两行（买入+卖出），备注"先卖后买"或"先买后卖"+预期价差

### 7.3 150 字总结

大盘 + **最强/最弱板块前 3（标注持仓/自选所属板块的强度）** + 操作核心（趋势/震荡品种代码+操作）+ 今日低吸机会（标的@价位）+ 未来 2-5 日展望（方向+关键事件）。

---

## 八、预判校准（收盘复盘）

| 判定 | 含义 | 动作 |
|---|---|---|
| 兑现 | 时间窗内到目标区 | 可了结部分 |
| 偏离 | 方向对但区间未到 | 顺延观察 |
| 失效 | 触发证伪 | 按 5.2 破位处理 |
| 到期 | 时间窗走完未到 | 降级"震荡"，减半/撤单 |

**连续 2 次失效/到期 → 移入观察名单 10 日**。
