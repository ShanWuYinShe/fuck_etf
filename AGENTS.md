# AGENTS.md（项目事实）

## 项目定位

A股ETF交易辅助：**脚本只负责采集数据，分析与消息面判断交给 AI**。完整规则见 `PROMPT_full.md`（含持仓/标的的数据源说明、消息面与操作纪律；规则历史版本见 git 历史）。

## 常用命令（跨平台，仅需 Python 3）

- 采集：`python3 scripts/fetch_etf_data.py`（实时三源、分时、日K、十一源快讯并行合并 [国内7源直连+国外4源走代理，代理在脚本顶部 PROXY 配置] 与重仓股公告、指数、板块、外盘、ETF 份额/重仓 → `.workwork/data/`）；盘后只刷消息面：`python3 scripts/fetch_etf_data.py --news-only`
- 晨间预案：`python3 scripts/morning_plan.py`（只采集数据，提示交给 AI 分析）
- 盘中监控：`python3 scripts/monitor.py`（`--once` 单轮 / `--until-close` 盘中会话，仅采集）

所有脚本均在 `scripts/` 目录，用 Python 标准库实现，可在任意目录、任意平台（Windows/macOS/Linux）运行。配置在 `config/` 目录（`holdings.json`/`etf_list.txt`），兼容根目录旧位置。

## 数据与配置

- `.workwork/` 全部为本机数据（已 gitignore，不入库）：`data/` 只存**最近一轮**采集结果，**不保留历史数据**——采集失败即删旧文件（`_manifest.json` 状态仅 ok/fail），每轮启动自动清理已移出自选的标的缓存；快讯每轮实时获取，AI 分析前若快讯滞后应先 `--news-only` 刷新。
- **解析健壮性约定**：免费接口 JSON 结构（key 名/层级）随时可能改版，解析一律走递归工具 `_find_lists`（按键名定位条目列表，不依赖固定路径）+ `_default_content_check`（写入前校验结构，漂移即拒收触发重试/换源）；消息源解析 0 条或结构校验未过会在 stdout 打印告警，AI 分析时若见「解析异常」告警应先修复再采。
- **入库数据源（git 管理，可跨设备同步）**：`config/holdings.json`（持仓唯一数据源，模板 `config/holdings.example.json`）与 `config/etf_list.txt`（自选标的清单，每行一个 6 位代码），兼容根目录旧位置。改持仓/成本/自选只改这两个文件，AI 每次分析前必须读取它们，不依赖提示词内旧数据,config/holdings.json中如果 batch 为空说明是空仓。
- 提示词 `PROMPT_full.md` **只含策略规则**（数据源说明、消息面与操作纪律），不内嵌持仓/标的/行情数据；AI 需自行读取 `config/holdings.json` 与 `config/etf_list.txt` 获取最新持仓与自选列表。

## 约束

- 输出全部使用中文；仅依赖 Python 3 标准库（urllib），无第三方包。
- 只采集数据与生成建议，禁止自动下单；分析结论由 AI 结合消息面生成、人工复核执行。
- 依赖免费公开行情接口，可能限流；采集脚本内置多级降级与重试。

## 关键规则速览（详见 PROMPT_full.md）

- **条件单参数一律用价差（元，3位小数），禁止百分比**：反弹买入（先跌至 X，反弹价差）、回落卖出（先涨至 X，回落价差）、止盈止损（按价格：盈利/亏损价差）；唯一例外为日涨跌幅单型（同花顺界面限定百分比）。
- **最小触发价差与止损上限按现价分档（元）**：<1元→触发≥0.001、止损≤0.011(T+1)；1–2元→≥0.002、≤0.020；2–5元→≥0.004、≤0.050；≥5元→≥0.008、T+0 止损≤0.090。
- **T+1 当日买入不可卖**：持仓 `buy_time` = 当日时，减仓/止损/止盈卖单一律延至下一交易日。
- **大盘环境三级（8.1）**：指数收于 20 日线上方=正常（盈亏比≥1.2、核心机会仓位≤1/2）；跌破/20日线拐头=谨慎（盈亏比≥1.5、仓位≤1/4、允许小仓新开）；连续3日线下方=保守（停新开仓、减至半仓以下）。**板块强度（2.1）叠加**：所属板块前 10 强/10 弱榜（`boards_up.json`/`boards_down.json`）定强/弱，板块退潮品种方向降级、低吸区下移/目标区下调。
- **盈利优先（5.3/8.0）**：低吸挂单贴近现价（MAX(5日低点,现价×0.99)）保证可成交；放量突破可轻仓追入（≤1/4，止损=突破位下方1档）；破位信号明确立即执行不等14:45；止盈分批（1/2目标区兑现+1/2移动止盈），禁止市价追在日内高点。
- **止损锚结构位**：止损价=关键支撑（10日低/平台/20日线）下方1个价差档；自动条件单作灾难兜底，破位信号明确立即执行；假破位（次日收回支撑上方）按 5.3 重新接回。
- **买入条件单无止损参数**：买入单（股价条件买入/反弹买入）只含触发价/方向/数量；止损由成交后另挂的止盈止损卖出单承担，买入单输出不写止损。
- **止损单唯一性**：同一笔持仓的止损只由一张卖出类单承担——止盈止损双向单（自带亏损腿）与股价条件止损单二选一，禁止重复挂止损单（防双重触发/数量冲突）。
- **禁止裸低吸/残缺做T**：低吸买入必须带盈利路径（目标区或成对做T卖出腿）；做T必须成对给买卖两腿+预期价差（元），先核算做T盈亏比 ≥1（否则判定"无T可做"）；持仓重/谨慎环境优先「持有+兜底+反弹减」，不主动制造交易。
- **条件单输出带代码+名称**：所有条件单/下单清单必须同时标注标的代码与名称（如「518880 黄金ETF华安」），禁止只写代码。
- **预判到期失效**：时间窗走完未到目标区→预判降级「震荡」、减半/撤单；连续2次失效/到期转观察名单10日。

## 分析会话标准流程（本机执行）

"搜集消息面 + 结合持仓/自选 + 分析未来几日走势 + 给今日最优操作" 每轮按此顺序执行（脚本只采集、AI 只分析，禁止自动下单）：

1. **采集**：`python3 scripts/fetch_etf_data.py`（盘后只刷消息面用 `--news-only`；午休 11:30-13:00、收盘后暂停采集）。
2. **读持仓/自选**：`config/holdings.json` + `config/etf_list.txt`（唯一数据源，兼容根目录，禁止用旧快照；持仓成本/份额/可卖以该文件为准）。
3. **消息面前置**：读 `news_merged.json`（十一源合并去重，取最新 30 条）+ `news_ann.json`（重仓股近 3 天公告）；滞后先 `--news-only` 刷新；按 PROMPT_full 思路对持仓/待交易品种做主动联网搜索补充（半导体/黄金/创新药/通信等）。
4. **板块走势判定（2.1）**：读 `boards_up.json` + `boards_down.json`（行业板块前 10 强/10 弱，`data.diff[]` 中 f14 板块名、f3 涨跌幅、f62 主力净流入；非交易时段 f3=0、f62="-" 属正常），把每只持仓/自选映射到所属板块 → 强度三级（强/中/弱/退潮），叠加到大盘环境与个股前瞻。
5. **大盘环境判定（8.1）**：`index_realtime.txt` + `index_kline_*.json` 算 20 日线 → 正常/谨慎/保守，写在输出最上方。
6. **趋势前瞻（5.3）**：对每只持仓/自选给 **2–5 日预判**（方向 / 低吸区 / 目标区 / 时间窗 / 证伪信号），以「消息面 + 趋势结构 + 板块强度」三要素推导，买卖方向由前瞻决定、盘面只定择时；板块退潮的品种方向降级、低吸区下移/目标区下调。
7. **今日操作**：按环境定仓位上限 → 持仓优先「高抛低吸做T压成本」（成对买卖两腿 + 价差元 + 盈亏比≥1）/ 反弹减仓 / 结构位兜底止损 → 输出 **7.1 下单清单**（代码+名称+方向+挂单价+数量+单型+止损+目标+盈亏比+触发条件）。T+1 卖出腿数量 ≤ 当日可卖份额。
8. **收盘复盘**：预判校准表（10.1，兑现/偏离/失效/到期）+ 给用户 `holdings.json` 更新建议（AI 不代写）+ 明日预案。
9. **文档维护**：分析框架/流程变动更新到本文档或 `PROMPT_full.md`；策略规则以 `PROMPT_full.md` 为准，本文件只记本机执行流程与速览。
